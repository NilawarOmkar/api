<%= form_with(model: api_response) do |form| %>
  <% if api_response.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_response.errors.count, "error") %> prohibited this api_response from being saved:</h2>

      <ul>
        <% api_response.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data, style: "display: block" %>
    <%= form.text_area :data %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
